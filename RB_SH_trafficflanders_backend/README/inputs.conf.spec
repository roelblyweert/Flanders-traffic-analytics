[traffic_inv_kvstore://default]
*Traffic Flanders to KV store

fetch_url = <value>
*This is the URL needed to make the actual HTTP call and fetch the inventory data.

dest_collection = <value>
*This is the destination KV store collection name where the inventory data needs to be stored.

